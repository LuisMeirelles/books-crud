meta {
  name: Get Token
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/token
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@example.com",
    "password": "password"
  }
}

vars:post-response {
  token: {{res.body.token}}
}
